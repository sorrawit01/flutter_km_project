import "package:flutter/material.dart";

class Home01UI extends StatelessWidget {
  const Home01UI({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.green,
    );
  }
}
